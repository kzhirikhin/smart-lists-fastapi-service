# Сборочная стадия. Здесь работает pip и остаётся всё, что он тянет за собой;
# в runtime-образ из этой стадии переезжают только сами пакеты.
FROM python:3.13-slim AS builder

COPY requirements.txt /tmp/requirements.txt

# --prefix собирает установленное в отдельный корень, который вторая стадия
# копирует одним слоем поверх /usr/local.
#
# --require-hashes: закреплённой версии мало. Версия говорит, какой релиз брать,
# но не что внутри него лежит, — подмена артефакта в уже выпущенной версии
# закреплением не ловится. Флаг избыточен, пока хеши есть в файле (pip включает
# проверку сам), и нужен ровно на случай, когда они оттуда исчезнут: тогда
# сборка упадёт вместо того, чтобы тихо поставить непроверенное.
RUN pip install --no-cache-dir --require-hashes --prefix=/install -r /tmp/requirements.txt

FROM python:3.13-slim

# Официальный образ пересобирается реже, чем Debian выпускает обновления
# безопасности: на 2026-08-20 в python:3.13-slim не хватало util-linux
# 2.41.5-0+deb13u1, уже выпущенного в trixie. Плавающий тег даёт последний
# опубликованный образ, а не последние пакеты, поэтому обновляем явно.
RUN apt-get update \
    && apt-get upgrade -y \
    && rm -rf /var/lib/apt/lists/*

# pip сервису не нужен ни для чего: зависимости ставит сборочная стадия. Зато
# при RCE это готовый установщик пакетов с доступом в сеть. Он же тащит
# собственные вендоренные библиотеки, которых нет в requirements.txt и которые
# иначе не убрать, — на 2026-08-20 это три HIGH в msgpack и setuptools.
RUN python -m pip uninstall -y pip \
    && rm -rf /usr/local/lib/python3.13/site-packages/pip \
              /usr/local/lib/python3.13/site-packages/pip-*.dist-info \
              /usr/local/bin/pip /usr/local/bin/pip3 /usr/local/bin/pip3.13

COPY --from=builder /install /usr/local

WORKDIR /app

# Явный список вместо `COPY . .`. Денилист `.dockerignore` перечисляет
# известное на момент написания и молча пропускает всё добавленное позже: так в
# образ уже попали каталог `.github` с адресами деплоя и проектная
# документация. Аллоулист этот класс ошибок закрывает целиком.
COPY app/ ./app/

# Непривилегированный пользователь. Cloud Run изолирует контейнер, и root
# внутри него сам по себе мало что даёт, а долгоживущих секретов внутри нет
# вовсе. Сужение прав остаётся потому, что не стоит ничего. Иллюзий оно не
# создаёт: доступ к metadata-серверу, то есть к личности сервиса, есть у любого
# процесса контейнера независимо от UID.
#
# Права на /app не выдаём: приложение только читает свои файлы, а на запись ему
# нужен лишь HOME, который создаёт --create-home. Порт 8000 выше 1024, поэтому
# для bind привилегии не требуются.
RUN useradd --create-home --uid 10001 appuser
USER appuser

EXPOSE 8000

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
