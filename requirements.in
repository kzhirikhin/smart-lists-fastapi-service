# Прямые runtime-зависимости — единственный файл, который правится руками.
# Всё остальное в requirements.txt разворачивает pip-compile, включая хеши.
# Команда пересборки — в AGENTS.md, раздел «Зависимости».
fastapi==0.141.1
google-genai==2.23.0
google-auth==2.58.0
pydantic-settings==2.15.0
requests==2.34.2
slowapi==0.1.10
uvicorn==0.52.4
