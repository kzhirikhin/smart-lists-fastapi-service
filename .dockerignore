# После перехода Dockerfile на явный `COPY app/ ./app/` этот файл больше не
# решает, что попадёт в образ, — он только сокращает build context. Поэтому
# здесь перечислено крупное и заведомо ненужное, а не всё подряд.
venv/
__pycache__/
*.pyc
*.pyo
.env
tests/
.pytest_cache/
bruno/
.git/
